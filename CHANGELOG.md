# Changelog

このプロジェクトの変更点をまとめたものです。形式はおおむね
[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従います。詳細な調査経緯・実機での
測定値などは [docs/design-notes.md](docs/design-notes.md) を参照してください(このファイルは
利用者向けの要約、design-notes.md は開発ログに近い詳細版という位置づけです)。

## [Unreleased]

### Fixed
- **gzip圧縮された`.vgm`ファイルが「不正なVGM」として無言でスキップされる不具合**:
  拡張子だけを見て解凍要否を判断していたため、`調査用/Ashura-SMS/*.vgm`のように
  **中身は実際にはgzip圧縮(vgz形式)なのに拡張子が`.vgm`のファイル**を生のVGMとして
  読もうとして`"Vgm "`マジックの検証に失敗し、OLEDの`Chips:`欄が`detecting...`のまま
  次の曲へ進んでいた(実機で発見、デュアルSN76489対応の検証中に別件として判明)。
  ファイル内容(先頭2バイトのgzipマジック`0x1F 0x8B`)を見て解凍要否を判定する
  `vgz_looks_like_gzip()`を追加し、拡張子ではなく中身で判断するよう`main.c`の
  `play_one()`を修正(`.vgz`なのに実は非圧縮、という逆のミスラベルにも同じロジックで
  対応)。`tools/host_tests/test_vgz_sniff.c`(新規)でユニットテストを追加、実際の
  Ashura-SMSファイルでの解凍も確認済み。実機確認済み(2026-09-26)。
- **Sega PCMで原曲にない余計なドラム音が曲の頭から最後まで鳴り続ける不具合**:
  `調査用/Space_Harrier_*.wav`(YM2203+Sega PCM、`02 Theme.vgm`)で発見。まず
  SPI転送側(1音9レジスタ分の書き込みタイミング)を疑い、`gap_us` 40→120(バイト破損対策、
  AY-3-8910の修正と同種)とバースト化(`gap_us=0`、レジスタ更新の非アトミック性=ティアリング対策)
  を実機で試したがどちらも改善せず。`vgmplay.ini`の`[segapcm] volume=0`でSega PCMだけを
  無音化して確認したところ、この音自体がSega PCM由来と確定。転送を一切介さないオフライン
  レンダラー(`tools/offline_render/render_segapcm.c`、新規)で同じVGM・同じレジスタ列を
  `chip_segapcm.c`だけで再生させたところクリーンな音声が出たため、チップのロジック自体は
  無罪と判明。残る真因は**曲頭のサンプルROM一括アップロード**(数千〜1万数千バイトを1バイト
  ずつ個別SPIフレームで送信)に、YM2612 PCMバンクアップロード(上記参照)と同じ再アンカー
  保護が入っていなかったこと: スレーブのコア間FIFOは満杯時に黙ってフレームを1つ捨てる
  非ブロッキング実装(同じくYM2612の項参照)なので、アップロード中に1バイトでも欠けると
  **それ以降の全バイトが1つずつズレて書き込まれ**、そのROM領域を参照する全チャンネルが
  曲の最後まで別データを再生し続けていた。`vgm_player.c`の`handle_data_block()`
  (`0x67` type=0x80)で、256バイトごとに絶対アドレスを再送するよう修正(YM2612側の
  128バイト間隔と同じ考え方、二重送信込み)。実機で改善を確認(2026-09-19)。
- **OLEDが起動画面(「starting...」)のまま更新されない不具合**: 音声は正常なのにOLEDだけ
  固まる。真因は`.vgz`展開用の`tinfl_decompressor`(約8KB、ハフマン復号テーブル3面)が
  core0の**スタック上のローカル変数**だったこと — core0のスタックは2KBしかなく、`.vgz`を
  1曲展開するたびに約6KBあふれて隣接する**core1のスタック全域**(RP2040のSCRATCH_X/Y配置で
  隣り合っている)を書き潰し、core1が次の関数リターンでゴミアドレスへ飛んでHardFaultし
  停止していた(音声は別コア/別タイミングなので無関係に鳴り続ける)。core1用の軽量
  HardFaultキャッチャ(`src/master/src/core_fault.c`、恒久的に同梱)で実際にフォールト
  したPCを捕捉して特定。修正は`tinfl_decompressor`を`static`化(.bssへ移動)するだけ
  (`src/master/src/vgz_inflate.c`)。core1の`printf`全廃・`sleep_ms`→`busy_wait_us`化は
  調査過程での妥当な補強だが、いずれも今回の真因そのものではなかった。
- **YM2612 PCM(メガドライブのドラム/ボイス)関連の一連の不具合**: 当初実装(下記Added)は
  マスター側でVGMサンプル単位のレートを計算してスレーブへ送る「フリーランのリサンプラ」
  方式だったが、スレーブの描画クロック(YM2612ネイティブ約53267Hz)とVGMのサンプルレート
  (44100Hz)の不一致、および`0x8n`の`n`ジッタを吸収できず、耳障りなノイズ・テンポのもたつき・
  ドラム欠落が実機で多数確認された。**チェックポイント方式に全面刷新**: マスターは
  約2msごとにバンク読み取り位置のチェックポイント(`DAC_SYNC`)だけを送り(従来の
  「毎`0x8n`1フレーム」の1/30以下のSPIトラフィック)、スレーブは自分の描画クロックで
  チェックポイント間隔を実測してレートを自己校正し、読み取り位置を前方向のみ・
  滑らかに補間する(読み取りインデックスを単調増加にして「既読バイトの再生」由来の
  クリック音を排除)。あわせて、PCMバンクの一括アップロード(曲頭に数千〜数万バイト)を
  128バイトごとの絶対カーソル再アンカーで頑健化(1フレーム欠落がバンク全体を壊さない)、
  電源投入直後のマスター/スレーブ起動タイミング競合(スレーブがSPIを聞き始める前に最初の
  PCMアップロードが送られ、そのスレーブだけ曲の間ずっと無音になる)への2秒の起動待機窓、
  スレーブのコア間FIFOをブロッキングから非ブロッキングに変更(満杯時はブロックしてSPI受信
  ハードFIFOを溢れさせる=バイト単位の破損よりも、フレーム単位で綺麗に捨てる方が安全)。
  実機で電源再投入・複数曲にわたり安定動作を確認(2026-09-11)。既知の残課題: バンク
  アップロード直後の最初のPCM発音だけ、音色がわずかに濁って聞こえるケースがある
  (`docs/design-notes.md`参照、対応中)。
- **YM2413が実機でRP2040だと音程が均一に低く聞こえる不具合**: RP2040標準クロックでは
  リアルタイム処理が追いつかないことを実機で確認(2026-09-05)。Pico 2(RP2350)へ書き込み
  直すだけ(追加のオーバークロックは不要)で解消。`firmware/pico2/slave_ym2413.uf2`を追加。
- **YM2413が時々音割れする不具合**: 出力の最終スケーリングのヘッドルーム不足(`>>1`)が原因。
  9チャンネル同時発音での実測に基づき`>>4`に修正(他のFM系スレーブと同水準に)。単音時の音量が
  下がる副作用あり(詳細は`docs/design-notes.md`)。
- VGMヘッダのクロックフィールド読み取りが、ファイルの実ヘッダ長(VGMデータ開始オフセット)を
  超えた位置を無条件に読んでいたため、メガドライブ系(データ開始0x40)のVGMで先頭のYM2612
  レジスタ書き込みバイト列をYM2203の非ゼロクロックと誤認識する不具合を修正。OLEDに存在しない
  はずの"YM2203"が表示される、および下記の「無効チップの自動スキップ」機能が該当曲を誤って
  スキップする原因になっていた(`調査用/04 - Water.vgm`で発覚)。
- OLEDステータス表示が特定条件下で初回フレームのまま固まる不具合の調査・対処(表示自体は
  ハードウェア/配線起因と判明した個体もあり、ソフト側は影響なしと確認したケースを含む)。
- VGMヘッダのクロック欄に実在しないような値(1kHz未満/100MHz超)が入っていたら無視する
  ガードを追加(古い変換ツールがヘッダの未使用拡張フィールドをゼロ埋めせず残すケース対策)。
  当初これが下記の無音バグの原因かと疑ったが、実際の原因は別だったと判明(下記参照)。
- **一部のVGMが(全チップ共通で)曲ごとまるまる無音になることがある不具合**: 曲の終わりに
  送るMUTEが、次の曲頭のRESET(3回送信で冗長化)を正常受信した時だけ解除される作りになって
  おり、そのRESETが3回ともSPIタイミングの巡り合わせで取りこぼされると、以降その曲の
  レジスタ書き込みは正しく届いているのに音声出力だけ0に固定されたままになっていた。
  ファイルの中身に依存しない現象で、同じファイルでもループ再生のたびに鳴る/鳴らないが
  変わりうる(YM2413で報告されたが全8チップ共通の設計上の穴)。MUTE/NOP以外の全オペコード
  受信で mute を解除するよう修正(`src/slave_common/src/slave_engine.c`、全スレーブ共通
  なので8スレーブすべて再ビルド)。
- **AY-3-8910の音量低下・ノイズ増加**: 一部チャンネルの音量バグ(内部フィルタがトーン/ノイズの
  ゲート後波形にかかっており高音チャンネルを1/6〜1/4に減衰させていた)と、曲冒頭のレジスタ
  書き込みバースト(15回程度、待ち時間ゼロ)がSPIの取りこぼしで化ける不具合の両方を修正
  (`src/slave_ay8910/src/chip_ay8910.c`、`src/master/src/vgm_player.c`、`slave_bus.c`の
  AY8910用`gap`を80→120us)。冒頭の書き込みは全て2回冗長送信(同じ値の再送は副作用なし)。
  実機で10曲以上、電源再投入を含め確認(2026-09-13)。
- AY-3-8910の出力を、チャンネルごとのレベル平滑化ではなく最終ミックス後にワンポール
  ローパスフィルタ(既定3000Hz)をかける方式に変更。特定楽曲(`調査用/04 Hot Summer
  Riding.vgm`)でAYの当たりが実機より硬く聞こえる問題を、別楽曲のハードアタックを
  潰さずに緩和(詳細な調査の変遷は`docs/design-notes.md`参照)。
- **(2026-09-15解決)** 上記楽曲でAYのメロディがSCCとズレて聞こえる件は、タイミングの
  バグではなく、**AYがSCCに対してこのボードの音声ミックスでは単純に大きすぎた**ことが
  原因と判明(実験的にAY出力を50%に減衰させたところ原曲と同じ聴こえ方になった)。
  マスタースケジューリング・両チップの描画レート・SPI信頼性など、あらゆるタイミング系の
  仮説は入念な検証の末すべて否定された(`docs/design-notes.md`参照)。
- 診断ツール`VGM_SLAVE_RATE_CHECK`自体の計測バグを修正: 1秒ごとの結果printfがUART
  (115200bps)にも出力され、その送信時間(~10ms)が次の1秒間の計測に混入し、実際には
  処理落ちしていないスレーブが約1%遅く見えていた(`src/slave_common/src/slave_engine.c`)。
- `tools/config_gui/vgmplay_config_gui.py`のデフォルトgap値表示: AY-3-8910とYM2151の値が
  入れ替わっていた(80/120 → 正しくは120/0)のと、YM2151の`0`(バーストモード)が
  Pythonの真偽値判定で「未設定」と誤判定されヒント文言が誤っていたのを修正。

### Changed
- 電源投入後、OLEDのタイトル/"starting..."画面を約1秒表示してから1曲目の再生を始めるように
  変更(以前はSDマウント等が速いため一瞬で再生が始まり、タイトルがほぼ見えなかった)。
- `firmware/` を **`pico1/`**(RP2040 / RP2040-Zero)と **`pico2/`**(RP2350)のフォルダに
  再編成。`pico1/`には実機でRP2040標準クロックでは性能不足と確認済みのスレーブ
  (YM2612 / YM2151 / YM2203 / YM2413)を同梱しなくなりました。それでもRP2040で試したい場合は
  ソースから`-DPICO_BOARD=pico`(既定)で再ビルドしてください(非推奨)。
- Sega PCMは当初YM2151と同様の懸念からPico 2版も用意していましたが、RP2040標準クロックで
  問題なく鳴ることを実機で再確認(2026-09-05)。`pico1/`側を正とし、`pico2/`側は参考として残しています。
- README: 各スレーブの書き込み先ボード(RP2040 or Pico 2)の記載を実機検証結果に合わせて修正
  (以前は全チップをRP2040-Zero向けと誤記していました)。

### Added
- **YM2612 DAC/PCM(メガドライブの「PCM」)対応**: これまでMDのドラム/ボイス(YM2612 6chの
  DACに8bit PCMを流し込むもの)は無音でした。`0x67` type-0のPCMバンクを曲頭にYM2612スレーブへ
  一括アップロードし、以降は約2msごとのチェックポイント(`DAC_SYNC`)だけを送るチェックポイント
  方式で実際のDAC供給はスレーブが自前で行います(設計の詳細・変遷は上記Fixedおよび
  `docs/design-notes.md`参照)。プロトコルに`VGMSPI_OP_YM2612_*`(7オペコード)を追加。
  バンク上限は既定192KB(`-DVGM_YM2612_PCM_BUDGET`で変更可)。汎用DACストリーム制御
  (`0x90`-`0x95`)は引き続き未対応。実機で再生確認済み(2026-09-11)。
- マスターが、`vgmplay.ini`で無効化されている(または対応スレーブを用意していない)チップを
  使う.vgm/.vgzを検出し、そのパートが欠けたまま再生するのではなく**曲ごとスキップ**する機能。
- ルートに`LICENSE`(MIT)を追加。同梱している依存コードのライセンス(ymfm: BSD-3-Clause、
  no-OS-FatFS-SD-SPI-RPi-Pico: Apache-2.0/FatFsライセンス、miniz_tinfl: MIT相当)は変更なし。
  ホスト側の比較検証専用コード(Nuked-OPM, LGPL-2.1)はビルド成果物に含まれず、リポジトリにも
  含めていません。
- 本CHANGELOG。
- **チップごとの出力音量調整(`vgmplay.ini`の`volume`キー)**: 複数チップの音声をアナログで
  混ぜる構成では、チップ同士のネイティブな音量差がボードの配線・回路次第で大きくばらつく
  ことがある(`04 Hot Summer Riding.vgm`のAY-3-8910/SCCで発覚、上記Fixed参照)。新しい
  SPIオペコード`VGMSPI_OP_VOLUME`(既定100=変更なし、0-255%)と`vgmplay.ini`の
  `volume = <0-255>`キーで、チップごとにボード単位で音量バランスを調整できるように
  なりました。`RESET`と一緒に冗長送信され、`slave_common`層(全8スレーブ共通)の
  `audio_pwm_write()`直前で適用されるので、個々のチップエミュレータ自体は無変更です。
  実機確認済み(2026-09-15)。
- **シャッフル再生(`vgmplay.ini`の`[player]`セクション、`shuffle`キー)**: 既定は
  従来通りアルファベット順(`shuffle = no`)。`shuffle = yes`にすると、SDカードの
  ルートを再生し直すたびに(`pico_rand`のハードウェア乱数で)ランダムな順に
  再生するようになります。設定ファイル・`tools/config_gui/`のGUIツール双方から
  編集可能。実機確認済み(2026-09-15)。
- **スキップボタンのGPIO変更(`[player] skip_button`)と冒頭プレビュー再生
  (`[player] preview` / `preview_seconds`)**: スキップボタンは既定でGPIO2固定
  だったが、クローン基板(例: 「USR」ボタンがGPIO24にある基板)向けに
  `vgmplay.ini`の`[player]`セクションで`skip_button = <0-28>`として変更可能に
  なった(適用は`slave_bus_init()`より前、SD再マウント直後)。あわせて
  `preview = yes`にすると、各曲を`preview_seconds`(既定30)秒再生した時点で
  スキップボタンを押したのと同じ扱いで次の曲へ進む「試聴モード」を追加
  (`vgm_player_elapsed_seconds()`をポーリングする形で実装、`vgm_player.c`本体は
  無変更)。`tools/host_tests/test_player_config.c`にキーの解析テストを追加。
  あわせて、`slave_bus.c`のCS予約ピン警告がスキップボタン移動後も追随するよう
  修正(`slave_bus_set_skip_button_gpio()`追加、固定だった`case 2`を撤廃)。
  `tools/config_gui/vgmplay_config_gui.py`のGUI/CLIにも同じ3キー
  (`skip_button`/`preview`/`preview_seconds`)を追加し、予約ピン判定も
  同様に動的化(`tools/config_gui/test_vgmplay_config_gui.py`にテスト追加)。
  実機確認済み(GPIO24へのボタン配線含む、2026-09-23)。
- **SDカードのフォルダを再帰的に走査する再生モード(`[player] recursive`)**:
  既定はこれまで通りルート直下の`.vgm`/`.vgz`のみ。`recursive = yes`にすると
  カード内の全サブフォルダを深さ優先で走査し、フォルダごとに全ファイルを
  再生してから次のフォルダへ進む(`shuffle`はフォルダ単位で適用、カード全体を
  混ぜて1つのリストにはしない)。`main.c`のcore0スタックが2KBしかない
  (OLED起動不良の原因になった過去のHardFault、上記参照)ことを踏まえ、素朴な
  再帰関数呼び出しは避け、ディレクトリパス・サブフォルダ名一覧を`depth`で
  引く`static`配列に置き、FatFsの`DIR`/`FILINFO`スキャン用ハンドルも
  レベル間で使い回す1個の`static`変数にすることで、再帰の深さ(既定上限4)に
  スタック使用量が比例して増えないようにした。基本動作は実機で確認済み。
  `tools/config_gui/vgmplay_config_gui.py`のGUI/CLIにも`recursive`キーを追加
  (`tools/config_gui/test_vgmplay_config_gui.py`にテスト追加)。
- **スキャン開始フォルダを指定する`[player] root_dir`**: 既定は""(SDカードルート)。
  `root_dir = GAMES/Sega`のようにSDカード相対パスを指定すると、そのフォルダを起点に
  走査するようになる(先頭/末尾の`/`や`0:`ドライブプレフィックスは自動除去)。
  `recursive`とは独立したキーで、`root_dir`単体ではそのフォルダ直下のファイルのみ、
  `recursive = yes`と組み合わせるとそのフォルダ以下を再帰的に再生する(=カード全体
  ではなく指定フォルダの中だけを再帰走査、という当初の要望どおりの動作)。
  存在しない/誤字のフォルダはエラーにせず「見つからない」扱いで再スキャンを続ける。
  `tools/host_tests/test_player_config.c`にキー解析・エイリアス(`rootdir`/`folder`/
  `dir`)・正規化のテストを追加。実機確認済み(2026-09-25)。
  `tools/config_gui/vgmplay_config_gui.py`のGUI/CLIにも同じ正規化ロジックと
  `root_dir`入力欄を追加(`tools/config_gui/test_vgmplay_config_gui.py`にテスト追加、
  長さ超過のバリデーションも実装側の`ROOT_DIR_BUF_SZ`と一致させた)。GUIには
  さらに**Browse...**ボタンを追加し、フォルダ選択ダイアログで選んだ絶対パスを
  (開いている、または保存先の)`vgmplay.ini`のあるフォルダを基準に自動で
  相対パス化する(SDカード外を選んだ場合はエラー表示)。
- **SN76489のデュアル構成対応(VGM仕様 "Dual Chip Support")**: VGM仕様では
  2個目のSN76489はコマンド`0x30 dd`(1個目の`0x50 dd`と同じデータ形式)で
  書き込む。`vgm_chips.h`に`VGM_CHIP_SN76489_2`を追加し、`vgm_player.c`の
  ディスパッチに`0x30`を追加してこの新チップへ配線。ヘッダのSN76489クロック
  フィールド(0x0C)のbit30(デュアルチップフラグ)を検出して使用チップ一覧
  (OLED表示・チップ欠落時の曲スキップ判定)にも反映。**副次的に見つかった
  既存バグの修正**: `hdr_clock()`のクロック値マスクが`0x7FFFFFFF`(bit31のみ
  クリア)になっていたが、VGM仕様のデュアルチップフラグは実際にはbit30
  (`0x40000000`)で、これを`0x3FFFFFFF`(bit30・31の両方をクリア)に修正 --
  修正前は、デュアルチップ宣言のあるVGMファイルは(2個目どころか)**1個目の
  SN76489自体が検出されない**不具合があった(クロック値がbit30混入で
  1億Hzのサニティ上限を超えて弾かれるため)。ハードウェアは1台目と全く
  同一(`slave_sn76489.uf2`をもう1枚書き込むだけ、新規ビルド不要)、CSは
  GPIO27に配線(既定`present=true`、未配線でも他チップ同様に無害)。
  `vgmplay.ini`に`[sn76489_2]`セクション、`tools/config_gui/`のGUI/CLIにも
  対応行を追加。`tools/host_tests/test_vgm_sn76489_dual.c`(新規)で
  ヘッダのデュアルチップ検出・コマンド`0x30`のディスパッチ・クロック
  プリセットの2台目への継承を検証、`test_player_config.c`と
  `test_vgmplay_config_gui.py`にも`[sn76489_2]`のエイリアス解析テストを
  追加。実機確認済み(2026-09-26、デュアル構成での再生を確認)。

**PicoChiptuneOrchestra** として初めて公開したスナップショットです(旧作業名
「VGMPlay 分散マルチMCU」から改称。[vgmrips/vgmplay](https://github.com/vgmrips/vgmplay)本家とは
無関係の独自実装です)。

マスター(Raspberry Pi Pico、SDカードからVGM/VGZを読みSPIで配信)+ チップごとのスレーブ基板
(SN76489 / YM2612 / YM2413 / YM2151 / YM2203 / AY-3-8910 / K051649(SCC) / Sega PCM)という
分散マルチMCU構成。master + 8スレーブすべてクロスビルド確認済み、複数チップ構成での実機動作も
確認済み。詳細は[docs/design-notes.md](docs/design-notes.md)参照。
