# VGMPlay分散マルチMCU構成 設計イメージ

マスター: Raspberry Pi Pico(RP2040) / スレーブ: RP2040-Zero × N台(各1〜2音源チップ担当)
通信: SPI

---

## 1. 全体構成

```
                        +-------------------------+
                        |   Raspberry Pi Pico      |
                        |   (マスター / RP2040)     |
                        |                          |
   SDカード ---SPI---→  |  ・VGMファイル読み込み     |
                        |  ・VGMコマンドパース       |
                        |  ・チップ別に振り分け       |
                        |  ・SPIでコマンド配信       |
                        +----+----------+----------+
                             |          |
                     SPI0(CS0)   SPI0(CS1)   ... SPI0(CSn)
                             |          |
              +--------------+  +--------------+       +--------------+
              | RP2040-Zero  |  | RP2040-Zero  |  ...  | RP2040-Zero  |
              | スレーブ#1    |  | スレーブ#2    |       | スレーブ#n    |
              | YM2612       |  | SN76489      |       | AY-3-8910    |
              | エミュレート   |  | エミュレート   |       | エミュレート   |
              |              |  |              |       |              |
              | I2S/PWM出力  |  | I2S/PWM出力  |       | I2S/PWM出力  |
              +------+-------+  +------+-------+       +------+-------+
                     |                 |                      |
                     +--------+--------+----------+-----------+
                              |
                      アナログミキサー(オペアンプ+抵抗)
                              |
                            スピーカー/ヘッドホン出力
```

**設計方針**
- マスターはVGM再生の「頭脳」に徹し、音声波形は生成しない
- 各スレーブは担当チップのレジスタエミュレーションと音声出力(DA変換)のみを行う
- 複数チップの音声はデジタルではなくアナログ段でミキシングする(クロック同期問題を回避するため)

---

## 2. マスター(Pico 1 / RP2040)の役割

| 機能 | 内容 |
|---|---|
| ファイルI/O | SDカードからVGM/VGZファイルを読み込み(SPI SDカードモジュール使用) |
| 展開 | VGZ(gzip圧縮)の場合は展開処理 |
| パース | VGMコマンドストリームを順に解釈(0x50=SN76489, 0x52/0x53=YM2612 port0/1, 0xA0=AY-3-8910 など) |
| ウェイト処理 | `0x61 nn nn`(nサンプル待機)、`0x62`(1/60秒)、`0x63`(1/50秒)をタイマーで管理 |
| コマンド振り分け | コマンドのチップ種別を見て対応スレーブのSPIチャンネルへ送信 |
| SPI配信 | 各スレーブに対しCS(チップセレクト)を個別に制御し、順次送信 |
| (任意)UI | ボタン入力での曲送り/一時停止、簡易ディスプレイへの曲名表示など |

### SPI配信のタイミング設計
- VGMコマンドは基本「即座にレジスタへ反映」が前提のため、マスターはウェイトを消化した瞬間に該当コマンドをSPI送出する
- 複数チップへ同時発行が必要な場合(同一タイミングで異なるチップへのコマンドが連続する場合)、SPIは1本のバスを複数CSで共有する設計だと逐次送信になるため、数十マイクロ秒単位のズレが発生しうる
  - 気になる場合はSPIバスをスレーブごとに分離(マスター側で複数SPIペリフェラルやビットバンギングを使う)することも検討可能
  - RP2040はSPIペリフェラルを2系統(SPI0/SPI1)持つため、3台以上なら一部はソフトSPI(PIO活用)で補うのが現実的

---

## 3. スレーブ(RP2040-Zero)の役割

| 機能 | 内容 |
|---|---|
| SPI受信 | マスターからのコマンドをスレーブモードSPIで受信(割り込みまたはPIOでキューイング) |
| レジスタエミュレーション | 受信したレジスタ書き込みを担当チップのソフトウェアエミュレータに適用 |
| 波形生成 | エミュレータ内部でサンプルごとの波形値を計算 |
| 音声出力 | I2S DACモジュール、またはPWM+RCフィルタでアナログ出力 |

### チップ選定と負荷の目安

| チップ | 特徴 | RP2040-Zero単体での負荷感 |
|---|---|---|
| SN76489(PSG) | 矩形波+ノイズ、実装が軽量 | 余裕あり |
| AY-3-8910(PSG) | SN76489よりやや複雑 | 余裕あり |
| YM2612(FM) | 6chオペレータ演算、負荷が高め | ギリギリ〜要最適化(固定小数点必須) |
| YM2413(FM簡易版) | YM2612より軽い | 比較的余裕あり |

→ FM音源(YM2612)は1チップだけで1スレーブを専有させるのが無難。PSG系は場合によっては1スレーブに2チップ同居も検討可能。

---

## 4. 通信プロトコル(SPI)の設計イメージ

### パケットフォーマット案(1コマンドあたり)

```
[1byte: コマンド種別] [1byte: レジスタ/アドレス] [1byte: データ] (+必要に応じ追加バイト)
```

例:YM2612 port0へのレジスタ書き込み
```
0x52  0x28  0xF0   → "YM2612 port0, レジスタ0x28に0xF0を書き込め"
```

- スレーブ側はコマンド種別を見て「自分宛か」を判定(あるいはCS配線で最初から物理的に宛先分離しているのでコマンド種別のフィルタは不要な設計も可)
- PCMデータストリーミング(0x8xコマンドやDACデータブロック)が絡む場合は転送量が増えるため、対応チップ・機能をどこまで含めるかは要検討(まずはFM+PSGレジスタ書き込みのみの対応から始めるのが現実的)

---

## 5. クロック同期に関する方針

- **サンプルクロックの完全同期はしない**方針とする(I2S BCLK/LRCLK共有によるハード同期は配線・設計コストが高く、フットプリント優先のコンセプトに反するため)
- 各スレーブは内蔵/外付けクリスタルベースで独立にサンプルレート(例: 44100Hz)を生成
- 音声はアナログ段(オペアンプ加算回路、または単純な抵抗ミキシング+バッファ)で合成
- 長時間再生でのわずかな位相ズレは実機の複数チップ構成に近い「味」として許容する

### 精度を上げたい場合の拡張案(将来検討)
- マスターが基準クロック(例: 44.1kHzパルス)をGPIOで全スレーブに配線し、各スレーブのサンプル生成をこの基準に同期させる
- この場合、配線が1本増えるだけなのでフットプリントへの影響は小さい

---

## 6. 開発ステップ案

1. **PoC(1マスター+1スレーブ)**: SN76489など軽量チップ1つで、SPI経由のコマンド配信とレジスタエミュレーション・音声出力の一連の流れを確立
2. **レイテンシ実測**: SPI送信からスレーブ側で音が変化するまでの遅延を計測し、音楽的に許容範囲か確認
3. **2チップ化**: PSG+FMなど異なる負荷のチップを組み合わせて、マスター側のスケジューリングを検証
4. **アナログミキシング回路の実装**: オペアンプ加算回路またはパッシブミキサーで複数スレーブの出力を合成
5. **3チップ以上への拡張**: SPIバス分割(SPI0/SPI1+ソフトSPI)や複数CS管理の実装
6. **対応VGMコマンドの拡充**: PCM/DACデータブロックなど、必要に応じて機能を追加

---

## 7. フットプリント・部品構成イメージ

| 役割 | 部品 | 個数 |
|---|---|---|
| マスター | Raspberry Pi Pico(RP2040) | 1 |
| スレーブ | RP2040-Zero | 2〜3(チップ数に応じて) |
| SDカード | SPI接続 microSDモジュール | 1 |
| DAC/フィルタ | I2S DACモジュール、またはPWM出力+RCローパスフィルタ | スレーブ数分 |
| ミキシング | オペアンプ(例: TL072等)+抵抗 | 1回路 |

RP2040-Zeroは21mm×18mm程度と非常に小型のため、3〜4台並べてもマスター(Pico本体)と合わせて手のひらサイズに収まる見込みです。

---

## 8. 主な検討事項・リスク

- SPIバス共有時のコマンド発行遅延がFM音源の音の立ち上がりに影響しないか(実測が必要)
- RP2040-Zeroのクリスタル有無・精度によるサンプルレートのばらつき
- GPIO数の制約(SPI通信用+I2S/PWM出力用ピンの両立)
- 電源設計(複数MCU+DAC回路の消費電流をどう配分するか)
- YM2612など負荷の高いチップのエミュレーション最適化(固定小数点演算、テーブル参照の高速化)
